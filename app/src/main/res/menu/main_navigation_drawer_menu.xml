<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ShowIn="navigation_view">
    <item android:id="@+id/navigation_pages" android:title="@string/app_name">
        <menu
            android:id="@+id/pages"
            android:checkableBehavior="single">
            <item
                android:id="@+id/home_page"
                android:checkable="true"
                android:icon="@drawable/main_home_icon"
                android:title="@string/app_name" />
            <item
                android:id="@+id/outbox_item"
                android:checkable="true"
                android:icon="@drawable/main_history_icon"
                android:title="@string/app_name" />
            <item
                android:id="@+id/favourites_item"
                android:checkable="true"
                android:icon="@drawable/main_authors_icon"
                android:title="@string/app_name" />
            <item android:title="@string/app_name" android:id="@+id/bookmarks"
                android:checkable="true"
                android:icon="@drawable/main_collections_icon"/>
            <item android:title="@string/app_name" android:id="@+id/collections"
                android:checkable="true"
                android:icon="@drawable/main_bookmarks_icon"/>
        </menu>
    </item>
</menu>